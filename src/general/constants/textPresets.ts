const EXTRA_SMALL_SIZE = 10;
const SMALL_SIZE = 12;
const DEFAULT_SIZE = 14;
const DEFAULT_WEIGHT = '100';
const HEADER_SIZE = 16;
const TITLE_SIZE = 24;

const FONT_SIZE = {
  xsmall: EXTRA_SMALL_SIZE,
  small: SMALL_SIZE,
  default: DEFAULT_SIZE,
  header: HEADER_SIZE,
  title: TITLE_SIZE,
};

export {
  FONT_SIZE as default,
  EXTRA_SMALL_SIZE,
  SMALL_SIZE,
  DEFAULT_SIZE,
  DEFAULT_WEIGHT,
  HEADER_SIZE,
  TITLE_SIZE,
};
